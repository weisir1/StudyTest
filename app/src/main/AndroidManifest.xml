<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lxhstudy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LxhStudy">
        <activity android:name=".item.Hencoder_ScaleActivity"></activity>
        <activity android:name=".item.MaterialEditTextActivity" />
        <activity android:name=".item.CameraAnimationAct" />
        <activity android:name=".item.RetrofitTestAcrivity"></activity>
        <activity android:name=".item.MyViewActivity" />
        <activity android:name=".item.SensorMagneticActivity" />
        <activity android:name=".item.SensorOrientationActivity" />
        <activity android:name=".item.SMSListenerActivity" />
        <activity android:name=".item.PhoneBlackList" />
        <activity android:name=".item.PhoneManagerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.PhoneService" />
        <service android:name=".service.InPhoneService" />
    </application>

</manifest>